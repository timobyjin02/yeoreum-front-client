const palette = {
  loginBtn: '#1d7cd3',
  fontGrey: '#555',
  fontBlack: '#181818',
  grey: '#dbdbdb',
  lightGrey: '#f5f6f7',
};

export type PaletteType = typeof palette;

const theme = {
  palette,
};

export default theme;
